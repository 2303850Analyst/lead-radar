// Test-only replacement for Next's marker package. Runtime bundling still uses
// the real `server-only` module; this file merely lets Node execute server code
// in the isolated evaluation process.
